name: arborio_sample
description: "Arborio Sample App"

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.2.3 <4.0.0"

dependencies:
  arborio:
    path: ..
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  flutter_colorpicker: ^1.0.3
  

 
dev_dependencies:
  austerity: ^1.1.0
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:

  assets:
    - assets/
    - assets/images/

  uses-material-design: true
